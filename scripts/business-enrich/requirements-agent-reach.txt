# Agent-Reach for optional business web enrich.
# Requires Python >= 3.10 (package constraint). Do NOT use PyPI `agent-reach==0.1.0`
# (different project). Install from GitHub:
#
#   python3.11 -m pip install -r scripts/business-enrich/requirements-agent-reach.txt
#
# yt-dlp pin in upstream may fail — if so:
#   python3.11 -m pip install --no-deps "git+https://github.com/Panniantong/Agent-Reach.git"
#   python3.11 -m pip install "requests>=2.28" "feedparser>=6.0" "python-dotenv>=1.0" "loguru>=0.7" "pyyaml>=6.0" "rich>=13.0"
#
# Runtime for KRUGI enrich uses Agent-Reach upstream tools directly
# (Jina Reader / Jina Search / optional mcporter+Exa) — same stack as agent-reach SKILL.md.

git+https://github.com/Panniantong/Agent-Reach.git
