{
  "adapter": "generic_apify_group",
  "actor_input_template": {
    "startUrls": [],
    "resultsLimit": 20,
    "viewOption": "CHRONOLOGICAL",
    "onlyPostsNewerThan": "2024-01-01",
    "proxyConfiguration": {
      "useApifyProxy": true
    }
  },
  "notes": [
    "Default Actor example: 2chN8UQcH1CfxLRNE (set APIFY_ACTOR_ID).",
    "Input uses startUrls[{url}] + resultsLimit; group URL is injected from FACEBOOK_GROUP_URL / --group-url.",
    "Profile enrichment: local author snapshot always (when enabled). Remote page scrape only with --fetch-profile-pages + FACEBOOK_PROFILE_ACTOR_ID.",
    "Cookies for closed groups: Apify secret storage or FACEBOOK_COOKIES_JSON only — never commit."
  ]
}
